import { createFileRoute } from "@tanstack/react-router";
import { createServerFn } from "@tanstack/react-start";
import { useEffect, useState } from "react";
import { sql } from "~/db";
import { getCurrentUser } from "~/lib/auth";

type Role = "estimator" | "proposal_writer" | "accountant" | "project_manager";
type Member = { id: number; email: string; role: Role; status: "pending" | "active" | "declined"; invited_at: string };
type Activity = { id: number; member_email: string; action: string; details: string | null; bid_title: string | null; created_at: string };
const roles: { key: Role; label: string; icon: string }[] = [
  { key: "estimator", label: "Estimator", icon: "📊" }, { key: "proposal_writer", label: "Proposal Writer", icon: "✍️" },
  { key: "accountant", label: "Accountant", icon: "🧮" }, { key: "project_manager", label: "Project Manager", icon: "📋" },
];
async function ensureTables() {
  await sql()`CREATE TABLE IF NOT EXISTS team_members (id SERIAL PRIMARY KEY, owner_id INTEGER NOT NULL REFERENCES users(id), email TEXT NOT NULL, role TEXT NOT NULL CHECK (role IN ('estimator','proposal_writer','accountant','project_manager')), status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','active','declined')), invited_at TIMESTAMPTZ DEFAULT NOW(), accepted_at TIMESTAMPTZ, UNIQUE(owner_id,email))`;
  await sql()`CREATE TABLE IF NOT EXISTS team_activity (id SERIAL PRIMARY KEY, member_email TEXT NOT NULL, action TEXT NOT NULL, bid_id INTEGER REFERENCES bids(id), details TEXT, created_at TIMESTAMPTZ DEFAULT NOW())`;
}
async function owner() { const u = await getCurrentUser(); if (!u) throw new Error("Not authenticated"); const p = await sql()`SELECT id FROM business_profiles WHERE user_id=${u.id} LIMIT 1`; if (!p.length) throw new Error("Only the business owner can access the workspace"); return u; }
const getWorkspace = createServerFn({ method: "GET" }).handler(async () => { const u = await owner(); try { await ensureTables(); } catch { return { allowed: true, members: [], activity: [] as Activity[] }; } const members = await sql()`SELECT id,email,role,status,invited_at FROM team_members WHERE owner_id=${u.id} ORDER BY invited_at DESC`; const emails = [u.email, ...(members as any[]).map(m => m.email)]; const activity = await sql()`SELECT a.id,a.member_email,a.action,a.details,a.created_at,b.title AS bid_title FROM team_activity a LEFT JOIN bids b ON b.id=a.bid_id WHERE a.member_email = ANY(${emails}) ORDER BY a.created_at DESC LIMIT 50`; return { allowed: true, members: members as Member[], activity: activity as Activity[] }; });
const inviteMember = createServerFn({ method: "POST" }).validator((d: unknown) => d as { email: string; role: Role }).handler(async ({ data }) => { const u = await owner(); await ensureTables(); const email = data.email.trim().toLowerCase(); if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) throw new Error("Enter a valid email address"); if (!roles.some(r => r.key === data.role)) throw new Error("Choose a valid role"); if (email === u.email.toLowerCase()) throw new Error("You cannot invite yourself"); const rows = await sql()`INSERT INTO team_members(owner_id,email,role) VALUES(${u.id},${email},${data.role}) RETURNING id,email,role,status,invited_at`; return rows[0] as Member; });
const removeMember = createServerFn({ method: "POST" }).validator((d: unknown) => d as { email: string }).handler(async ({ data }) => { const u = await owner(); await ensureTables(); await sql()`DELETE FROM team_members WHERE owner_id=${u.id} AND email=${data.email}`; return { success: true }; });
function relative(date: string) { const mins = Math.floor((Date.now()-new Date(date).getTime())/60000); if (mins < 1) return "just now"; if (mins < 60) return `${mins} minute${mins===1?"":"s"} ago`; const hours=Math.floor(mins/60); if(hours<24)return `${hours} hour${hours===1?"":"s"} ago`; const days=Math.floor(hours/24); return days===1?"yesterday":`${days} days ago`; }
function activityText(a: Activity) { const who = a.member_email.split("@")[0]; const title = a.bid_title || "a bid"; if(a.action === "scored_bid") return `${who} scored a bid${a.details ? ` — ${a.details}` : ""} on ${title}`; if(a.action === "drafted_proposal") return `${who} drafted a proposal for ${title}`; if(a.action === "saved_bid") return `${who} saved ${a.details || "a bid"} to track`; if(a.action === "dismissed_bid") return `${who} dismissed ${title}`; return `${who} ${a.details || a.action}`; }
export const Route = createFileRoute("/workspace")({ loader: () => getCurrentUser(), component: WorkspacePage });
function WorkspacePage() { const [data,setData]=useState<{members:Member[];activity:Activity[]}|null>(null); const [error,setError]=useState(""); const [invite,setInvite]=useState<Role|null>(null); const [email,setEmail]=useState(""); const [busy,setBusy]=useState(false); const load=()=>getWorkspace().then(r=>setData(r as any)).catch(e=>setError(e.message)); useEffect(()=>{load()},[]); async function send(){if(!invite)return;setBusy(true);setError("");try{await inviteMember({data:{email,role:invite}});setEmail("");setInvite(null);await load()}catch(e){setError(e instanceof Error?e.message:"Invite failed")}finally{setBusy(false)}} async function remove(member:Member){if(!confirm(`Remove ${member.email}?`))return;await removeMember({data:{email:member.email}});load()}
 return <main className="min-h-screen bg-slate-50"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-5"><div><a href="/dashboard" className="text-sm font-semibold text-blue-600">← Dashboard</a><h1 className="mt-2 text-3xl font-bold text-slate-900">Team Workspace</h1><p className="mt-1 text-slate-500">Invite collaborators and track your team’s progress on every opportunity.</p></div><a href="/" className="font-bold text-slate-800">Contrax</a></div></header><div className="mx-auto max-w-6xl space-y-10 px-6 py-10">{error&&<div className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</div>}<section><div className="mb-4 flex items-end justify-between"><div><h2 className="text-xl font-bold text-slate-900">Your team</h2><p className="text-sm text-slate-500">Assign roles to keep bids moving forward.</p></div></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{roles.map(role=>{const m=data?.members.find(x=>x.role===role.key);return <div key={role.key} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="text-3xl">{role.icon}</div><h3 className="mt-3 font-bold text-slate-900">{role.label}</h3>{m?<div className="mt-4"><p className="truncate text-sm text-slate-600" title={m.email}>{m.email}</p><span className={`mt-2 inline-block rounded-full px-2.5 py-1 text-xs font-semibold ${m.status==='active'?"bg-emerald-100 text-emerald-700":"bg-amber-100 text-amber-700"}`}>{m.status}</span><button onClick={()=>remove(m)} className="mt-4 block text-xs font-semibold text-red-600 hover:text-red-700">Remove member</button></div>:invite===role.key?<div className="mt-4 space-y-2"><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@company.com" type="email" className="w-full rounded-lg border border-slate-300 px-3 py-2 text-sm"/><select value={role.key} onChange={e=>setInvite(e.target.value as Role)} className="w-full rounded-lg border border-slate-300 px-3 py-2 text-sm">{roles.map(r=><option key={r.key} value={r.key}>{r.label}</option>)}</select><div className="flex gap-2"><button disabled={busy} onClick={send} className="rounded-lg bg-blue-600 px-3 py-2 text-xs font-bold text-white">{busy?"Sending…":"Send Invite"}</button><button onClick={()=>setInvite(null)} className="text-xs text-slate-500">Cancel</button></div></div>:<button onClick={()=>setInvite(role.key)} className="mt-5 w-full rounded-lg border border-dashed border-blue-300 py-2 text-sm font-semibold text-blue-600 hover:bg-blue-50">+ Invite</button>}</div>})}</div></section><section><h2 className="text-xl font-bold text-slate-900">Activity</h2><p className="mb-4 text-sm text-slate-500">AI-tracked progress across your bids and proposals.</p><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">{!data?.activity.length?<p className="py-8 text-center text-sm text-slate-500">No activity yet. Score, save, or draft a proposal to see progress here.</p>:<div className="space-y-5">{data.activity.map(a=><div key={a.id} className="flex gap-4"><div className="mt-1 h-3 w-3 shrink-0 rounded-full bg-blue-500 ring-4 ring-blue-50"/><div><p className="text-sm font-medium text-slate-800">{activityText(a)}</p><p className="mt-1 text-xs text-slate-400">{relative(a.created_at)}</p></div></div>)}</div>}</div></section></div></main> }
